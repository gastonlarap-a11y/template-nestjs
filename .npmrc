# pnpm configuration
# Hoist peer deps so NestJS/Prisma/Passport resolve cleanly without manual peer wiring.
auto-install-peers=true
strict-peer-dependencies=false
# Keep the store linked (default) for fast, disk-efficient installs under OrbStack volumes.
prefer-frozen-lockfile=false
